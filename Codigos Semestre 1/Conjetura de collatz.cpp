#include <iostream>

using namespace std;
int main()
{
    int n;
    cout << "ingrese un numero entero (positivo): ";
    cin >> n;

    for (in)
    while (true)
    {
        if (n % 2 == 0)
        {
            n = n / 2;
        }
        else if (n % 2 != 0)
        {
            n = (n * 3) + 1;

        }
        if (n == 1)
        {
            break;
        }
        cout << n << endl;
    }
    cout << n << endl;
}