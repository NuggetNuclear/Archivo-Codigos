#include <iostream> 

using namespace std; 

int main() 
{
    cout << "Hola mundo" << endl; 

    /* Comentarios multilinea */

    // Comentarios de una linea
    
   /* 
       cout = "Console Out", es lo que devuelve la consola;  
       << = Separadores, separan una funcion de lo que esta hace
       "" = Encierran texto, indican al programa que dentro de ellos hay texto puro que no debe interpretar
       endl = funcion de c++, significa que la consola salta a la proxima linea, es un "Enter"
       \n = Igual que endl, pero este va dentro del texto entre "", y no nesecita separadores
   */

   // ----------------------------------------------------------------------------------------------------------------


   /* 
        Tipos de variables

        int = enteros (numeros enteros incluyendo negativos); float = flotantes (numeros decimales); double = flotantes (Se usan solo cuando hay muchos decimales)
        string = palabras (Caracteres); bool = verdadero o falso
   */

  // Inicialización
  // Las variables tienen que ser inicializadas en el codigo (se les debe asignar un valor, ya sea por teclado o por defecto)

  int a; // Declara una variable de tipo entero con el nombre de a, esta no tiene ningun valor asignado
  int b = 1; // Declara una variable de tipo entero con el nombre de b, esta tiene asignado el valor de 1

}