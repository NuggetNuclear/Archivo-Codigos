#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    int x, y;
    x = pow(25,0.5); //pow(a,b) = a elevado a b
    y = sqrt(25); //sqrt(a) = raiz cuadrada de a
    
    cout << x << endl;
    cout << y << endl;
}
